# Daso Site

Single-page invitation site for the May 12 Daso parent conversation.

## Local Preview

Open `index.html` in a browser.

## Signup Form

The current form uses a no-backend `mailto:` handoff to `max@maxforsey.com`
because no Tally or Typeform URL was provided. Replace the form in `index.html`
with a Tally or Typeform embed when that form is ready.
